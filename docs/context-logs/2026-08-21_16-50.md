# Context Log — 2026-08-21 16:50

## 1. context-monitor 훅 재알림 가드

`.claude/scripts/context-monitor.ps1`

- 최근 저장 로그 판정 기준 15분 → **10분**으로 변경
- 로그 디렉터리 경로를 상대경로 `docs/context-logs` → `$PSScriptRoot` 기준 절대경로로 고정
  - **root cause**: 훅 실행 cwd가 프로젝트 루트가 아니면 기존 상대경로 가드가 통째로 무력화됨
- 검증: 직접 실행 → 최근 로그 존재 시 출력 없이 `exit 0`

## 2. README 재구성

`README.md` 335줄 → 105줄. 5개 섹션으로 축약:
소개 / 서비스 구성 / 핵심 아키텍처(mermaid 1장) / 실행 방법 / 핵심 링크

- 기존 전문(00~15 섹션)은 사용자가 만든 `README - 복사본.md` 백업에 있었고,
  이를 `docs/architecture/auth-gateway-design.md`로 이동 후 README에서 링크
- 실행 방법은 `CLAUDE.md` 기준(`./gradlew build`, `bootRun --args='--spring.profiles.active=dev'`, 필수 환경변수 7개)
- Live Demo 3개 URL은 핵심 링크 하위로 유지

## 3. 아키텍처 다이어그램 렌더링 검증

`npx @mermaid-js/mermaid-cli`로 실제 SVG/PNG 렌더링 확인.

- 문법 오류 없음, 노드 7개(Client + 6) 전부 출력
- 한글 · `<br/>` · `→` · `·` 문자 정상
- `~~~` 정렬 링크, `-.->` JWKS 점선 화살표 정상 동작
- `style` 색상 5개 의도대로 적용

## 미완료 / 후속

- `docs/architecture/auth-gateway-design.md` 내부 정리 미실행
  (제목이 여전히 "Auth Gateway Platform" + 배지 블록 그대로 — README와 중복)
- 변경사항 커밋 안 함 (branch: `develop`)
